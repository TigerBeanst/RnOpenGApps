package com.jakting.opengapps.utils








class DownUtils {

    /*var button_fab: MaterialButton? = null
    private var download_url = ""
    private var file_name = ""
    private var file_nameURL = ""
    private var context: Context? = null

    fun DownUtils(
        download_url: String?,
        context: Context?
    ) {
        this.download_url = download_url!!
        this.context = context
        file_name = GetFilename.get(download_url)!!
        //this.file_nameURL = context.getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS).getPath()+"/RnOpenGApps/"+ file_name;
        file_nameURL =
            Environment.getExternalStorageDirectory().toString() + "/Download/RnOpenGApps/" + file_name
        Log.d("debug", "DownUtils: 下载路径" + file_nameURL)
    }

    fun downloadGApps(context: Context?){
        task = DownloadTask.Builder(download_url, parentFile)
            .setFilename(filename) // the minimal interval millisecond for callback progress
            .setMinIntervalMillisCallbackProcess(30) // do re-download even if the task has already been completed in the past.
            .setPassIfAlreadyCompleted(false)
            .build()
    }*/
}